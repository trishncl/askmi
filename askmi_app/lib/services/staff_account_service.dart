// PHASE 2 — Auth core
// TO-DO: Creates/deletes Manager & Cashier accounts using a TEMPORARY secondary Firebase app (not a Cloud Function — see the Blaze-plan decision). Owner-only, enforced by firestore.rules isOwner().
//
// See: AA_Lomi_Firestore_Schema.docx (data shape) and the project roadmap
// (phase order) from earlier planning. Do not build this before its phase.
