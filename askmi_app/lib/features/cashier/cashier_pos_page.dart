// PHASE 5 — Manager & Cashier (Cashier POS, build last)
// TODO: Menu grid (reads menuItems for the cashier's branch) + cart + payment method + checkout. Checkout writes to 'sales' with cashierUid/cashierName and updates 'products' movement.
//
// See: AA_Lomi_Firestore_Schema.docx (data shape) and the project roadmap
// (phase order) from earlier planning. Do not build this before its phase.
