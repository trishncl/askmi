// PHASE 2 — Auth core
// TODO: Root routing widget: not signed in -> Landing/Login; signed in -> role switch (Owner/Manager/Cashier shell) using UserProfileProvider. Include the 'account not configured' fallback — never default to any role on lookup failure.
//
// See: AA_Lomi_Firestore_Schema.docx (data shape) and the project roadmap
// (phase order) from earlier planning. Do not build this before its phase.
